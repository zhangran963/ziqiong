{
  "name": "ziqiong",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "vite": "vite",
    "dev": "wrangler pages dev --proxy 5173 -- npm run vite",
    "dev:local": "wrangler pages dev --proxy 5173 -- npm run vite",
    "dev:build": "npm run build && wrangler pages dev dist",
    "build": "vite build",
    "deploy": "npm run build && npx wrangler pages deploy dist",
    "preview": "vite preview",
    "thumbnail": "node scripts/generate-thumbnails.js",
    "thumbnail:lyrics": "node scripts/generate-thumbnails.js lyrics"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.986.0",
    "dotenv": "^17.2.4",
    "vue": "^3.5.24",
    "vue-router": "^5.0.2"
  },
  "devDependencies": {
    "@unocss/vite": "^66.6.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "unocss": "^66.6.0",
    "vite": "^7.2.4",
    "wrangler": "^4.63.0"
  }
}
