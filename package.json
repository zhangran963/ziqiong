{
  "name": "ziqiong",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "vite": "vite",
    "dev": "vite",
    "dev:local": "wrangler pages dev --proxy 5173 -- npm run vite",
    "dev:remote": "wrangler pages dev --remote --proxy 5173 -- npm run vite",
    "build:wasm": "cd image-processor && wasm-pack build --target web && cp pkg/image_processor.js ../functions/api/ && cp pkg/image_processor_bg.wasm ../functions/api/ && ls -lh ../functions/api/image_processor*",
    "build:web": "vite build",
    "build": "npm run build:wasm && npm run build:web",
    "deploy": "npm run build:wasm && npx wrangler pages deploy dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@unocss/vite": "^66.6.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "unocss": "^66.6.0",
    "vite": "^7.2.4",
    "wrangler": "^4.63.0"
  }
}
